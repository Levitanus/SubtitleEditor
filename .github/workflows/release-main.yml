name: Release from main

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      short_sha: ${{ steps.meta.outputs.short_sha }}
      release_tag: ${{ steps.meta.outputs.release_tag }}
      release_name: ${{ steps.meta.outputs.release_name }}

    steps:
      - name: Compute release metadata
        id: meta
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "release_tag=main-${SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "release_name=Main build ${SHORT_SHA}" >> "$GITHUB_OUTPUT"

  build:
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: macos-latest
            platform: macos
          - os: windows-latest
            platform: windows
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release --locked

      - name: Package release artifact (unix)
        id: pkg_unix
        if: runner.os != 'Windows'
        shell: bash
        run: |
          ARCH="$(echo "${RUNNER_ARCH}" | tr '[:upper:]' '[:lower:]')"
          ASSET_NAME="subtitle_editor-${{ matrix.platform }}-${ARCH}-${{ needs.prepare.outputs.short_sha }}.tar.gz"
          tar -czf "$ASSET_NAME" -C target/release subtitle_editor
          echo "asset_name=$ASSET_NAME" >> "$GITHUB_OUTPUT"

      - name: Package release artifact (windows)
        id: pkg_windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $arch = "$env:RUNNER_ARCH".ToLower()
          $assetName = "subtitle_editor-windows-$arch-${{ needs.prepare.outputs.short_sha }}.zip"
          Compress-Archive -Path "target/release/subtitle_editor.exe" -DestinationPath $assetName -Force
          "asset_name=$assetName" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Upload packaged artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-assets-${{ matrix.platform }}
          path: ${{ steps.pkg_unix.outputs.asset_name || steps.pkg_windows.outputs.asset_name }}

  release:
    needs: [prepare, build]
    runs-on: ubuntu-latest

    steps:
      - name: Download packaged artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: release-assets-*
          merge-multiple: true
          path: dist

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.prepare.outputs.release_tag }}
          name: ${{ needs.prepare.outputs.release_name }}
          prerelease: true
          generate_release_notes: true
          files: |
            dist/*
